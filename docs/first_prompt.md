i've been using a rust based TUI for most of my agentic coding recently. i've landed on using it as a wrapper around the pi cli agent (docs here https://pi.dev/docs/latest, source here https://github.com/badlogic/pi-mono/tree/main/packages/coding-agent) to enable mutliple agents to work simultaneously on a project, via git worktrees. i manage the workflows for adding new features / implementing changes using openspec (details https://github.com/Fission-AI/OpenSpec). i manage bug fix / issue resolution workflows via issues managed on my self hosted forgeo server (but the system should support github too). my problem is that the current TUI isn't structured with this workflow tightly coupled. i've provided a screenshot of the current TUI i'm using. i want to build something similar but that runs in a direct client / server connection rather than having to first connect to the host that runs the TUI via ssh, as this means i can't use many hotkeys as they clash with the hotkeys of the terminal application i'm using. the UI layout i want is the same as what is shown in the screenshot i've shared here, and the workflow is: user runs the TUI on their workstation (macbook is primary device) and connects to the backend server address that is configured (a shared key secures access). on first run they would be prompted to add a project. this can be a plain local directory, local git repo, or provide a repo url (in which case they're prompted where to clone the repo to). if they're a returning user they can resume working on existing workspaces, selecting them from the list of active sessions on the left sidebar (active sessions as also in "tabs" across the top of the screen and can be cycled through via "cmd+`" or "cmd+shift+`" or directly selected via "cmd+1/2/3/etc" ; or navigate the side bar to select a project (or cycle through selected project using "opt+`" / "opt+shift+`" and pressing enter on "New workspace" or using the "cmd+n" hotkey on the selected project. when a new workspace is launched it first displays a list of open issues in the project's repo, filters are available; assignee, labels, etc. if they select an issue the context of the issue is fed into the agent and it will first create a new git branch with an appropriate name (referencing the issue number and a short <40 character description of the problem). the user can skip the issue list and would then be shown a list of openspec changes with incomplete tasks (with the count of remaining tasks displayed). filters and ordering options are available on the spec list. if a user selects a spec, the context is fed to the agent and the worktree branch gets an appropriate name. the user can choose to skip selecting a spec, where they'd then be shown a list of the available "skills" or "prompt templates" that can be used to prime the context of an agent in the new workspace. or they can skip all these options and just launch a "clean" workspace where they can explore the project and interact with the coding agent.  when they're ready to work on something the agent would prompt them to either use /opsx:propose or log an issue. it is crucial that when a new workspace is created, it is branched off an up to date default git branch (ie the local default branch should be in sync with remote). please ask me questions to clarify any parts of the project, i want you to have a clear understanding of the project's goal and use cases.